/**
 * Simplified Toaster component that works with our stub implementation
 * Rule: Functional component
 */
export function Toaster(): JSX.Element {
  // Just returning a placeholder component to prevent TypeScript errors
  return (
    <div className="toaster">
      {/* We'll implement the actual toast UI later */}
    </div>
  );
}
